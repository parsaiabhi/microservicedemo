server.servlet.context-path=/zuul
server.port=8762
spring.application.name=zuulserver
eureka.client.service-url.default-zone=http://localhost:8761/eureka/
eureka.client.registerWithEureka=true
eureka.client.fetchRegistry=true

# A prefix that can added to beginning of all requests. 
#zuul.prefix=/api

# Disable accessing services using service name (i.e. gallery-service).
# They should be only accessed through the path defined below.
# Link: https://stackoverflow.com/questions/46317388/zuul-service-name-exposed-instead-of-route-path-only
zuul.ignored-services=*

# Map paths to services
zuul.routes.display.path=/zuuldisplay/**
zuul.routes.display.url=http://localhost:9092/displaybalance
zuul.routes.display.service-id=display

# http://localhost:8762/zuul/zuuldisplay/displaybalance/client/customers/accounts/UOB70493326262193/balance

# Map path to auth service
#zuul.routes.auth-service.path=/auth/**
#zuul.routes.auth-service.service-id=AUTH-SERVICE

# By default, all requests to display service for example will start with: "/zuuldisplay/"
# What will be sent to the display service is what comes after the path defined, 
# So, if request is "/gallery/view/1", gallery service will get "/view/1".
# In case of auth, we need to pass the "/auth/" in the path to auth service. So, set strip-prefix to false
zuul.routes.auth-service.strip-prefix=false

# Exclude authorization from sensitive headers
#zuul.routes.auth-service.sensitive-headers=Cookie,Set-Cookie 



